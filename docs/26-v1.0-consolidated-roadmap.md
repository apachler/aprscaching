# v1.0.0 — Consolidated Release Roadmap

**Project:** aprscaching.com (greenfield revival) · **Owner:** OE8APR
**Doc type:** Master plan — the single ordered guide from "where we are" to a shippable **v1.0.0**.
**Status:** ACTIVE working doc. *This file is itself a planning artifact* — it is **retired in the
release-prep stage** (§Stage 4), its substance folded into the README / CHANGELOG / marketing copy.
**Reads with:** all of `docs/` (notably `docs/27` — the heritage-adoptions analysis that sources O6–O9),
the three `.claude/rules/*`, and `CLAUDE.md`. It consolidates the open items across every doc into one
dependency-ordered plan; per-feature detail stays in the home doc.

---

## 0. How to use this

Work the stages **top to bottom**. Each stage lists: its goal, the exact open items it closes (with
the source doc), dependencies, the sub-steps, and a Definition of Done. Every sub-step is meant to be
**independently committable and CI-green across the tri-runtime** (Worker/D1 · Node/SQLite · Bun), per
the project's existing bar. Nothing here re-opens a decision already ACCEPTED in `docs/14`.

---

## 1. Baseline — what is already DONE (the floor)

This is large; the platform is far past MVP. Confirmed built + tested as of this doc:

- **Spine & core (M0–M9):** caching CRUD, the A/B/C verification engine (`verify.ts`), import/heritage,
  community/gamification, workbench depth, the tri-runtime + dual-runtime CI.
- **Identity/auth (`docs/10`, `docs/19`) — COMPLETE:** passkey + email accounts, multi base-call
  accounts (switch/add/verify, change-callsign), APRS message-challenge + LoTW control-verification,
  leaderboard gated on verification, `account_stations` SSID registry. *S1–S5 done.*
- **Federation (`docs/06`, `docs/15`) — COMPLETE incl. the launch-gating tier:** F0–F3 live; **F4 peer
  trust tiers + quarantine + corroboration quorum + ADR-5 signed tombstones (launch-gating) — COMPLETE**;
  F5 gossip + generalized signed feed + capability negotiation; F6 federated catalog + account-move +
  `fed_scope` redaction; F7 multi-key rotation + signed registry + observability. *Only F5(2)
  push-to-hub is deferred.*
- **ADRs (`docs/14`) — all ACCEPTED + IMPLEMENTED:** ADR-1 leaderboard-by-callsign, ADR-2 finds follow
  the account, **ADR-3 AGPL §13 Source link (`/.well-known/source`, was launch-blocking) — done**,
  ADR-4a free read API + keys, ADR-4b push + email-digest, ADR-5 tombstones.
- **Map/UX (`docs/11`):** M1 vector basemap + layer switcher + save/share view + favorites/search;
  **M2** topo/satellite (EOX/OpenTopoMap), ruler + range rings + grid/MGRS, day/night terminator +
  home-QTH bearing arc, enriched as-you-type search; **M3** track history + time-replay, GPX/KML/ADIF
  export, public read API; **M4** embed widget + QR, push alerts, API keys.
- **Weather (`docs/17`) — W1–W4 COMPLETE:** Ecowitt/WU ingest → `-13`; gated APRS WX beacon TX; CWOP/
  NOAA relay; browser-direct Ultimeter/Peet-Bros over Web Serial.
- **RF hardware (`docs/16`) — H1–H6 COMPLETE (Path B browser):** USB KISS, signed ingest, BLE-KISS,
  Meshtastic deframe, soundcard AFSK modem, gated TX, Web Serial CAT one-click tune. Path A ingest box
  (IGate/digi/CoT/Meshtastic-JSON) built.
- **Spots & remote control (`docs/20`):** S1–S3 live activity spots (POTA/SOTA/GMA/DX/RBN/PSK),
  R1–R2 remote box control, watchlist + alerts, spot filters.
- **Sustainability (`docs/12`) — through M5:** AGPL/MIT/CC-BY-SA licensing, supporter recognition +
  hide-nag, public `/support` ledger (4 buckets incl. peer-reimbursement), Open Collective link.
- **Profiles (`docs/13`):** thin opt-in ham profile, operated-SSID stations, corroborator board +
  infrastructure recognition, become-a-cache, cache-owner loop.
- **Landing/onboarding (`docs/18`) — mechanics only:** L2–L4 (landing gate, Explore, config-driven
  reduced-motion quick-tour framework) IMPLEMENTED with **placeholder content**.
- **BBS:** Stage-1 store-and-forward mail + bulletins; bulletin **federation** (BID-deduped over signed
  feeds); inbox polish (Sent tab, unread, reply, subject).
- **AX.25 connected mode (`docs/25` P0) — DONE:** `packages/ax25` LAPB state machine + frame codec,
  fully unit-tested.
- **Deployment (`docs/23`):** Topology 0 (Bun single-binary desktop) built; topologies 1–4 are
  validate-at-deploy.

> Implication: there are **no hard launch blockers left** (source link done; federation trust done).
> What remains for "feature-complete as the sum of docs intends" is a contained, ordered set below.

---

## 2. The open scope — everything left, classified

**In scope for v1.0.0 (build these, in the Stage order of §3):**

| # | Item | Source | State |
|---|------|--------|-------|
| O1 | Map M5: station **telemetry/weather graphs** (uPlot), **raw per-station packet view** (workbench), **"navigate to cache"** hand-off | `docs/11` M5 (+M2 navigate) | not built |
| O2 | **Packet radio P1–P4**: terminal · FBB BBS · forwarding/hierarchical routing · NET/ROM node + digipeater + sysop admin | `docs/25` | P0 done; P1–P4 open |
| O3 | **Two dark-only themes — Modern + Cogmind — with the Cogmind = late-90s "flip"** (a single merged stage: theme tokens **and** the retro terminal shell land together; switching to Cogmind *is* the flip). Drops light mode from `docs/24` scope. | `docs/24` + `docs/25` P5 | PLANNED, not built |
| O4 | **Landing/onboarding content** + the **official marketing splash** (copy, feature sections, screenshots, animated insights, real tour steps) | `docs/18` | mechanics done; content deferred |
| O5 | **Release engineering**: repo cleanup, README, SPDX, code-quality pass, v1.0.0 tag | this doc · `docs/14` ADR-3 | pending |
| O6 | **APRSCaching fidelity cluster F-1…F-8** (virtual cache type · WebNFC stage unlock · cache media via R2 · living-cache rendezvous log · PSK31/CW audio decoders · owner-gated rating · 10-char locator · drive-in/country/tags) — restores the character of the original concept | `docs/27` Part A | not built |
| O7 | **Graphic Packet hardware adoptions** — GPLSL-style **driver abstraction** (WA8DED/TFPCX hostmode · AGWPE client · PC/FlexNet) · **RTS/DTR PTT** · **autorouting + reversible learned paths + node tree** · **Multiport** per-port MYCALL | `docs/27` Part B.1 (+`docs/16`) | not built |
| O8 | **Graphic Packet terminal UX** — function-key macros · CTEXT/QTEXT · the **`NAMES.GP` station-type registry** (colourised by type) · an **ANSI-colour subset** in the terminal pane | `docs/27` Part B.2 | not built |
| O9 | **The "Tools" plugin/scripting system** — sandboxed, capability-gated Lua/JS plugins extending the terminal/BBS/workbench (heritage: GP's parser-DLL + GPRI `//PW` scripting). The F-5 PSK31/CW decoders are its first built-ins. | `docs/27` Part B.3 | not built |

**Explicitly deferred / reserved (NOT v1.0.0 — by design, do not let these block the tag):**

- **CI pipelines + workflows** — owner's explicit instruction: *"follow afterwards."* (Stage 5.)
- **AX.25 P0.b**: modulo-128 (SABME) + SREJ — extends the mod-8 core; only needed by high-throughput
  links. Slot before P2 *iff* a partner needs it; otherwise post-1.0.
- **RF-over-internet (`docs/22`)**: AXIP/AXUDP, HAMNET, AMPRNet/44net — **reserved seams**; the
  `provenance` abstraction is already in place. Tier-A-via-RF stays deferred (needs owned OE RF).
- **CAT Backend B (`docs/21`)**: Hamlib `rigctld` companion + Capacitor mobile shell — the 200+ rig
  long tail / iOS. Browser CAT (Backend A) ships in v1.
- **Native Meshtastic protobuf over MQTT/BLE/serial (Path A), AGWPE** (`docs/16`) — browser-direct
  Meshtastic ships in v1; the always-on native path is post-1.0.
- **Federation F5(2) push-to-hub** (`docs/15`) — pull-sync ships; push optimization later.
- **Deployment topologies 1–4 CI exercise** (`docs/23`) — validate-at-deploy; Topology 0 ships.

---

## 3. The ordered plan

**Ordering rationale.** (1) Finish cheap loose ends first (derisk, complete the map story) **and the
APRSCaching fidelity cluster O6** (it restores the original concept's character and several pieces —
F-5 decoders — seed the Tools system). (2) Build the **packet stack (P1–P4)** with the **GP hardware +
terminal-UX adoptions (O7/O8)** folded in — the largest remaining capability and the foundation both
the retro shell and the Tools system need. (3) Build the **Tools plugin system (O9)** once the terminal
exists (its first extension surface). (4) Ship the **two themes + the flip (O3)** as a *single* merged
stage — the Cogmind theme tokens **and** the retro terminal shell land together, because switching to
Cogmind *is* the late-90s flip; only do it once the component set (incl. packet + Tools surfaces) is
stable, since a theme re-skins finished surfaces. (5) **Release prep last**, so screenshots/marketing
reflect the final, themed product. CI is explicitly post-1.0.

**Theme decision (supersedes `docs/24` light-mode scope):** v1 ships **exactly two themes, both
DARK-ONLY — Modern** (the current default visual language) and **Cogmind** (the late-90s green-screen
flip). There is no light mode. Switching theme to Cogmind performs the retro shell transformation in
one motion; there is no separate "retro shell" stage decoupled from the theme.

### Stage 0 — Loose ends + APRSCaching fidelity (completes the map story + restores the original concept)
*Closes O1 + O6. No hard dependencies (F-5 decoders pair the H4 audio pipeline). Detail: `docs/27` Part A.*

> **Status: ✅ DONE.** Shipped as ten commit-per-slice changes (migrations 0029–0035), tri-runtime
> green (gateway vitest 131 · Node smoke 190 · geofence 8 · web typecheck+build). Slices: 0.1 telemetry
> & weather graphs (uPlot) · 0.2 raw per-station packet view · 0.3 navigate-to-cache · F-1 virtual type ·
> F-2 WebNFC stage unlock + manual fallback · F-3 cache media (R2) · F-4 living-cache rendezvous ·
> F-6 owner-gated 1–5 rating · F-7 10-char Maidenhead · F-8 drive-in/country/tags. **Two deliberate
> scope calls:** F-8 `country` is owner-set (coordinate-derivation deferred until a geocoder is wired —
> not faked); F-4 rendezvous is a *social* record kept OUT of the A/B/C find tiers + leaderboard
> (anti-gaming). F-5 PSK31/CW decoders intentionally deferred to Stage 2 (they ship as the first Tools).
- **0.1 Telemetry & weather graphs** — add `uPlot` (tiny, dependency-light) charts to the station
  detail (workbench): temperature/humidity/pressure/wind from `sensor_readings`, and speed/altitude/
  course over the position track. Reuse the existing `getStationTrack` + a new `sensor_readings`
  windowed read. Honors reduced-motion; lazy-loaded so the cacher surface stays light (`css.md`).
- **0.2 Raw per-station packet view (station detail)** — a live + recent raw TNC2 frame log per station,
  in the station detail sheet (opened from a station pin; kept off the primary cacher flow per ui-ux).
  Back it with a hard-TTL `packets_recent`
  store (cost rule); the browser RF panel already renders live frames — add the per-station history.
- **0.3 "Navigate to cache"** — a one-tap field action on the cache sheet: bearing/compass readout
  (we already compute the great-circle bearing) + hand-off links to device maps / OSRM directions.
- **0.4 Fidelity cluster (O6 / `docs/27` Part A)** — restore the original concept's character:
  **F-1** add a `virtual` cache type (location/riddle/landmark, no container); **F-2** WebNFC stage
  unlock (Android Chromium) with a manual-code fallback; **F-3** cache **media attachments via R2**
  (images/audio/files on a cache + its stages, owner-managed, size/type-limited); **F-4** the
  **living-cache rendezvous log** (two opted-in living caches co-located + both beaconing in a window
  mutually log — same verify engine); **F-6** owner-gated rating (or formalise favourites as rating —
  decide); **F-7** accept/display **10-char Maidenhead**; **F-8** `drive_in` flag + country (from
  coords) + free tags. *(F-5 PSK31/CW decoders land as built-in **Tools** in Stage 2 — they need the
  Tool host.)*
- **DoD:** charts render in both density modes; raw view TTL-bounded; navigate hand-off works on
  mobile; virtual/NFC/media/rendezvous/rating/locator/tags all exercised; tri-runtime green.

### Stage 1 — Packet radio P1–P4 + the Graphic Packet adoptions (the long pole)
*Closes O2 + O7 + O8. Depends on `packages/ax25` (P0, done). Each phase ships independently + CI-green.
Detail lives in `docs/25` §8 + `docs/27` Part B.1/B.2.*
- **P1 — Browser packet terminal** ("Graphic Packet reborn") — **✅ DONE** (new MIT `packages/packet`:
  NAMES.GP registry · ANSI parser · multi-channel TerminalSession, 21 tests; browser Web Serial KISS
  transport + `PacketTerminal` workbench UI with channels/monitor/status/command-line/macros/CTEXT;
  GPLSL drivers in `apps/ingest` — KISS · AGWPE · WA8DED hostmode · RTS/DTR PTT · Multiport, codecs
  unit-tested, serial/engine behaviour validate-at-deploy; built token-driven for the Stage-3 flip).
  Original spec: multi-channel connected-mode terminal
  over Web Serial/KISS (then BLE/audio) driving `ConnectedLink`; channels, a monitor pane, status line,
  function keys, command line. Modern shell first. **Fold in the GP terminal UX (O8):** function-key
  **macro bar** (callsign/date/channel placeholders), **CTEXT/QTEXT** auto-text, the **`NAMES.GP`
  station-type registry** (tag heard calls BBS/node/DX/digi/weather/our-SSIDs → monitor colour +
  connect-menu grouping), and an **ANSI-colour subset** in the terminal pane (theme-token-mapped).
  *Milestone demo: connect to a real BBS/node.*
- **P1.h — GP hardware adoptions (O7 / `docs/27` Part B.1, `docs/16`):** add a **GPLSL-style driver
  abstraction** to `apps/ingest` so one terminal drives several back-ends — a **WA8DED/TFPCX hostmode**
  transport and an **AGWPE client** transport **alongside KISS**; **RTS/DTR-as-PTT** over Web Serial for
  the no-TNC TX path; **Multiport** per-port MYCALL/SSID. (Browser Web Serial where the device is USB-CDC.)
- **P2 — FBB-style BBS uplift** — **✅ DONE** (migration 0036 thread tree `reply_to`/`thread_id`; gateway
  SR reply-threading + `T` typing + `GET /api/bbs/thread/:id`; the pure FBB command interpreter
  `packages/packet/bbs.ts` — L/R/S/SR/K/H/B/X over a MessageStore, 6 tests; web P/B/T compose +
  threaded reply. The interpreter↔RF-connect wiring lands with the P4 node server / is validate-at-deploy.)
  Original spec: connected-mode BBS *server* (ingest) + the classic command interface
  over the existing message base; add the **thread tree** (`reply_to`/`thread_id`, `SR` reply, threaded
  views); P/B/T typing, MID/BID, expert mode.
- **P3 — Forwarding + hierarchical routing tree** — **✅ DONE** (`packages/packet/forward.ts` —
  H-address parser + most-specific-prefix `ForwardRouter` + FBB proposal/accept codec, 4 tests; migration
  0037 `bbs_forward_rules` (bulletin federation seeded as the `ip-fed` catch-all) + `white_pages`;
  gateway routing + WP steering + sysop rule CRUD. rf-fbb link + LZHUF validate-at-deploy.) Original
  spec: parse `category @ bbs.#region.state.country.cont.WW`;
  `bbs_forward_rules` longest-prefix routing; the FBB forward protocol (proposal/accept, LZHUF) for
  `rf-fbb` partners; **fold today's bulletin federation in as the `ip-fed` partner**; White Pages for
  P-mail steering.
- **P4 — NET/ROM node + connected-mode digipeater + sysop admin** — **✅ DONE** (`packages/packet/
  netrom.ts` — NodesTable (learn/best-route/decay) + reversePath (GP reversible learned paths) +
  NodeSession CLI, 4 tests; migration 0038 `netrom_nodes` + `node_mheard`; gateway recordMheard (wired
  into ingest) + NODES list/sysop-add + MHeard; web read-only NodePanel. Connected-mode/alias
  digipeater + driving the CLI over RF connects are validate-at-deploy.) Original spec: `NetRom` L3/L4
  (NODES table,
  circuits, routing); the node user CLI (Nodes/Routes/Connect/Users/MHeard/CQ/Bye); connected-mode +
  alias digipeating (extends `packages/aprs/digipeat.ts`); the sysop **admin/config** surface. **Fold
  in GP autorouting (O7):** connect-by-alias auto-routing, **reversible learned digi paths**, and an
  **AX.25 node/route tree** view; optional cross-connect ("durchconnect").
- **(Optional P0.b here)** modulo-128/SREJ in `packages/ax25` if a real partner needs throughput.
- **DoD per phase:** pure cores unit-tested; RF behaviour validate-at-deploy (hardware-path posture);
  operator-local (ingest-locality); RX stays Tier C (verify.ts untouched).

### Stage 2 — The "Tools" plugin/scripting system (`docs/27` Part B.3)
*Closes O9. Depends on Stage 1 (the terminal/BBS are its first extension surfaces).*

> **Status: ✅ DONE** — impl doc `docs/28`. New MIT `packages/tools` (capability model · `tool.json`
> validation · `ToolHost` with per-tool capability enforcement + injected TX gate · built-ins ·
> F-5 CW+PSK31 decoders), **12 tests**. Web `ToolsPanel` (built-ins off-by-default + permissions, a
> decode box, a command runner, import-by-URL with a permission prompt) + a locked-down **Web Worker
> sandbox** for imported command-tools. **Runtime call:** JS Worker, not Lua/wasmoon (dependency-light,
> testable; runtime-agnostic host so Lua can drop in later).
> **Extended well past the original scope (all DONE):** a function-agnostic host + inter-tool **IPC bus**
> (pub/sub + named services + surface-provided services, per the GPRI-validated "host routes, never
> interprets" invariant); **20 built-ins** incl. the GP-archive set (info/QTH/conv/CW/away-note/connect-bell/
> link-ping/station-DB) + **GPAUTO scripted sessions** + **GIP block-art** (a `blocks` panel node); **full
> imported-tool parity** (commands, IPC, colourisers, panels, decoders all cross the Worker bridge); and
> **signed manifests + a signed tool registry/marketplace** (Ed25519, trust-on-first-use, `tools/toolkey`
> authoring CLI — `docs/28` §7). **Remaining Tools follow-ons (post-1.0):** the Web Audio DSP front-ends
> that feed live signal into the CW/PSK31 decoders, and a `map` host surface (capability declared, no
> renderer yet).
- Build **"Tools" — sandboxed, capability-gated plugins** (Lua via `wasmoon`/Fengari *or* a locked-down
  JS worker; both MIT, no ambient network/DOM/FS). A Tool is a **signed manifest + script** (`tool.json`:
  name, author callsign, permissions, entry).
- **Host API (extension points):** register a `/command` in the terminal/BBS; hook events
  (`on_frame`/`on_connect`/`on_disconnect`/`on_beacon`/`on_find`/`on_spot`); contribute monitor
  filters/colourisers; provide decoders; schedule beacons; add a small theme-aware panel/overlay or
  declarative map layer. Versioned, a11y + reduced-motion inherited.
- **Built-in set (ships curated):** the **F-5 PSK31 + CW (Morse) decoders** (Web Audio, reusing the H4
  pipeline — this is where O6's F-5 lands), an **auto-responder** (GP PMS-style), a **CTEXT macro pack**,
  a **monitor colouriser** (drives off the `NAMES.GP` registry), a **beacon scheduler**.
- **Guardrails:** capability grants are explicit; anything that **transmits** still passes the existing
  **H5 / control-verification** TX gates; a Tool can **never** bypass `verify.ts` trust. CPU/time
  budgeted; off by default, opt-in per Tool. (Heritage: GP's parser-DLLs + GPRI `//PW` scripting.)
- **DoD:** the built-in decoders + auto-responder run sandboxed; import-by-URL prompts for permissions;
  no Tool can transmit without the TX gate; tri-runtime green; warrants its own impl doc when scheduled.

### Stage 3 — Two dark-only themes (Modern + Cogmind) — and the Cogmind = late-90s "flip" (`docs/24` + `docs/25` P5)
*Closes O3 — a single merged stage. Depends on Stage 1 (packet surfaces) + Stage 2 (Tools surfaces);
best done now that the whole component set is stable. **Supersedes `docs/24` light-mode scope.***

> **Status: ✅ DONE.** Two dark-only themes ship — **Modern** (default) + **Cogmind** — as a token layer
> (`buildCogmindStyle()` re-skins the MapLibre map too), switched from Settings → Display (`["modern",
> "cogmind"]` segmented control; persisted via `account_prefs`). Selecting Cogmind *is* the flip:
> `[data-theme="cogmind"]` phosphor tokens across every surface + `[data-shell="terminal"]` CRT-window
> transform (full-width box-drawing frames) on the packet/workbench apps; the `NAMES.GP` monitor colouriser
> + ANSI pane carry the terminal character. **CRT FX** (scanline/glow) is opt-in + reduced-motion-killed; a
> **CP437/VGA webfont** lazy-loads only in Cogmind; the emoji→ASCII **de-emoji sweep + CI guard**
> (`apps/web/test/no-emoji.mjs`) keeps Cogmind pure-ASCII. Verified end-to-end via the Cogmind desktop
> teaser (`tools/teaser/run-cog-teaser.sh`). The standalone `aprscaching-tui` read-API client + extra T3
> HUD flourishes stay post-1.0 (`docs/24` T3 / §7e).
- Ship **exactly two themes, both DARK-ONLY**: **Modern** (the current default visual language) and
  **Cogmind** (the late-90s green-screen flip). **No light mode.** Both are the **token layer** —
  same components, re-skinned.
- **The flip is one motion:** switching theme to **Cogmind simultaneously** applies (a) the **Cogmind
  token theme** — phosphor palette, CP437/box-drawing, monospace, "all info visible" HUD across landing,
  map, sheets, forms, settings, profile, BBS, workbench, **and the MapLibre map** (a half-themed app
  looks broken) — **and** (b) the **retro terminal shell** (`data-shell="terminal"`): command line +
  monitor pane + status line + function-key bar + box-drawing channel windows over the packet/workbench
  surfaces. There is no separate retro-shell toggle; **theme = shell**. The `NAMES.GP` colourised
  registry + the ANSI terminal (Stage 1) are half its visual character.
- The command line **augments** the buttons (never replaces; keyboard/AT parity is *better*, not worse).
- **Locked design decisions (`docs/24` §1a, from GP/FBB heritage research):** palette = green base +
  disciplined DOS accents (cyan/amber/red/white) only where colour carries meaning (tiers, station
  types, TX/RX); coverage = context-aware but **total** (every element incl. header/menu/controls is
  themed; packet/BBS surfaces additionally get the full ANSI box-drawing-windows treatment); CRT FX =
  **off by default, opt-in**; a bundled **CP437/VGA webfont** loads only with the Cogmind theme. P1's
  terminal is built token-driven so this stage is a token swap, not a rebuild.
- Hold the non-negotiables in `docs/24` §2 + `css.md`/`ui-ux.md`: AA contrast in green, **reduced-motion
  kills CRT/scanline motion**, real semantics + focus, field-use mission intact.
- **DoD:** the user can switch Modern↔Cogmind; both are coherent dark themes with no broken map;
  selecting Cogmind transforms BBS/terminal/node into the green-screen experience in one motion; a11y +
  reduced-motion preserved; the marquee "transform to the late 90s" demo works end to end.

### Stage 4 — Release prep for v1.0.0  *(the owner's "last point")*
*Closes O4 + O5. Do LAST, when the product (features + theme) is final so captures are final.*

- **4a — Repo cleanup / retire planning artifacts.**
  - **Archive the planning docs**: the superseded/working specs (`docs/01–04` v1/v2/v3/reborn plans,
    `docs/06` (superseded by 15), `docs/07` ui-audit, `docs/08` (superseded by 16), `docs/09`, the
    PROPOSED/PLANNED specs, and **this roadmap**) move to `docs/decisions/` (or a `docs/archive/`) as
    historical record, or are removed. Keep only docs that document **shipped** behaviour.
  - **Strip planning jargon from code + user-facing strings**: replace `docs/NN`, `M4/H5/P0/Sx/Wx`
    milestone references and "TODO/deferred" notes in **comments** with plain-English intent; remove
    milestone labels from **UI copy** (a ham sees features, not our phase names). Comments stay —
    the *scaffolding vocabulary* goes.
  - **SPDX headers** (ADR-3 follow-through): per-file `SPDX-License-Identifier` headers across the
    units (AGPL app/gateway, MIT libs, CC-BY-SA docs); verify `packages/*` stay MIT-clean.
  - **Prune** teaser scratch outputs, dead scaffolding under `deploy/` not yet exercised, stale
    `_*.mts` validation scripts, unused assets.
- **4b — Landing/onboarding content + the official marketing splash** (`docs/18` content pass).
  - Real **hero copy + feature sections** for a ham audience (what APRScaching is; the A/B/C trust
    story; the workbench breadth). Real **quick-tour steps** (the framework already exists).
  - **Screenshots + animated insights**: regenerate with `tools/teaser` against the **final, themed**
    app — modern shell, the Cogmind theme, and the **modern↔late-90s flip** as the showcase reel.
    Surface them on the splash + README.
  - **L1 — `.com` → `.net` 301 redirect** (pre-auth, edge) + rpId binding to `.net` only — a
    **deployment** task (record it; it gates the public domain story, not the build).
- **4c — README.md (+ per-package READMEs) for a ham operator.** A newcomer ham should grasp, fast:
  *what it is* (APRS-caching + a full APRS workbench), *why* (open, trust-by-corroboration not
  transport), the **A/B/C trust model** in one paragraph, the **workbench** breadth (RF/BBS/node/CAT/
  weather), **how to run / self-host** (the topologies, the Source link / AGPL), licensing, and links.
  No internal milestone vocabulary.
- **4d — Code-quality & tag.** Consistent inline docs; dead-code + stray-TODO sweep; lint/format pass;
  final **conformance green on all three runtimes** + every test suite; a `CHANGELOG.md` summarizing
  the feature set; version bump to **1.0.0**; **git tag `v1.0.0`**.
- **DoD:** a stranger ham reads the README and "gets it"; no `docs/NN`/milestone jargon leaks to users
  or code comments; SPDX clean; marketing splash live with final captures; `v1.0.0` tagged.

### Stage 5 — Post-1.0 (explicitly after the tag)
CI pipelines + workflows (owner: *"follow afterwards"*); then, as appetite allows, the reserved seams:
AX.25 P0.b, native Meshtastic/AGWPE (Path A depth), CAT Backend B (`rigctld` + Capacitor), AXIP/HAMNET/
44net (`docs/22`), F5(2) push-to-hub, deployment topologies 1–4 in CI, Tier-A-via-owned-RF.

---

## 4. Dependency graph & one-glance checklist

```
P0 (done) ─► Stage 1: P1 (+GP UX/hw) ─► P2 ─► P3
                                   └──────────► P4 (+autorouting)
Stage 0 (independent: loose ends + fidelity O6) ───────────┐
Stage 1 (packet surfaces) ─► Stage 2 (Tools plugin O9) ─┐  │
                                                        ├──┴─► Stage 3 (two dark themes + the "flip")
(stable component set) ─────────────────────────────────┘              │
                                                                        ▼
                                              Stage 4 (release prep: cleanup · splash · README · tag)
                                                                        │
                                                                        ▼
                                              Stage 5 (post-1.0: CI · reserved seams)
```

- [x] **0** Map M5 (telemetry/weather graphs · raw packet view · navigate-to-cache) + fidelity O6 (virtual · WebNFC · cache media · rendezvous · rating · 10-char locator · drive-in/tags) — **DONE**
- [x] **1** Packet P1 terminal (+GP macros/CTEXT/`NAMES.GP`/ANSI · +GP hostmode/AGWPE/PTT/Multiport drivers) → P2 FBB BBS → P3 forwarding/routing → P4 node/digi + admin (+autorouting/learned-paths/tree) — **DONE** (P0–P4; RF link behaviour validate-at-deploy)
- [x] **2** "Tools" plugin/scripting system (sandboxed JS Worker; built-ins: PSK31/CW decoders, auto-responder, colouriser, beacon scheduler) — **DONE** (`packages/tools` + web ToolsPanel; `docs/28`)
- [x] **3** Two dark-only themes (Modern + Cogmind) — selecting Cogmind *is* the late-90s flip (theme + retro shell in one) — **DONE** (Settings toggle · phosphor tokens + `data-shell="terminal"` · CRT FX opt-in · CP437 webfont · de-emoji CI guard · Cogmind teaser)
- [ ] **4a** Cleanup: archive planning docs · strip jargon from comments/UI · SPDX · prune scratch
- [ ] **4b** Landing content + marketing splash (final screenshots + animated flip reel) · L1 redirect
- [ ] **4c** README for a ham operator (+ per-package)
- [ ] **4d** Code-quality pass · conformance green · CHANGELOG · **tag v1.0.0**
- [ ] **5** (post-1.0) CI pipelines, then reserved seams

---

## 5. Definition of "feature-complete for v1.0.0"

Every doc's **in-scope** deferred item is built (§2 top table): the map story closed + the APRSCaching
fidelity cluster restored (Stage 0 / O6); the packet world live with the GP hardware + terminal-UX
adoptions (Stage 1 / O2+O7+O8); the Tools plugin platform shipped with its built-in decoders (Stage 2 /
O9); the app on **two dark-only themes** where selecting Cogmind *is* the late-90s flip (Stage 3 / O3);
a real landing + marketing splash (Stage 4b); and the repo released-grade (Stage 4a/c/d). The §2
"deferred/reserved" list is **knowingly excluded** — those are reserved seams or owner-deferred (CI),
not gaps.
At `v1.0.0` the trust model is **Tier B (app-geo) + honest Tier C**, with Tier A designed-for and
RF-deferred (`docs/22`) — a conscious, documented choice, not an omission.

## 6. Risks & sequencing notes

- **Packet (Stage 1) is the long pole** and the highest-risk RF code. P0's pure, tested core de-risks
  it; keep every new layer (P2–P4) pure-core + scripted tests, with hardware as validate-at-deploy.
- **Theme + flip last, after packet *and* Tools.** The flip is now a single merged stage (theme tokens
  + retro shell together); re-skinning a moving target is waste, so freeze the component set (Stages
  1+2) before Stage 3.
- **Two dark themes only — no light mode.** This supersedes `docs/24`'s light-mode scope; don't build a
  light variant for v1.
- **Release prep strictly last.** Screenshots/marketing must show the final themed product, incl. the
  flip — so Stage 4b can't precede Stage 3.
- **Don't gate the tag on reserved items.** §2's deferred list (esp. CI, AXIP/44net, Backend B) is
  post-1.0 by design; shipping 1.0 without them is correct, not incomplete.

---

## 7. Addenda from the full documentation sweep (reconciliation)

A complete read of `docs/01–25` confirms §1–§6 and adds these:

**7a. The docs' own status labels lag reality — reconcile them in cleanup (Stage 4a).** Several specs
still read `PLANNED`/`BUILDING` (e.g. `docs/03`/`04` M1–M5, `docs/16` H1–H6, `docs/17` W1–W4) although
the **code + git history show them shipped** (this roadmap's §1 is the ground truth, verified against
the tree, not the docs' stale headers). Stage 4a must **update or archive each doc's Status line to
match what's actually built** before release — otherwise the docs misrepresent the product.

**7b. Operator UI (`docs/09`, "M8") is effectively DONE.** NavRail, the desktop 3-pane docking, Nearby
cards/search, and the map coordinate readout are built. No action beyond the 7a status reconcile.

**7c. Small open config bits → fold into Stage 0.**
- `docs/19` (APRS-IS identity): wire the peer **service identity** config — `SERVICE_SSID` + the
  outbox `BBS_CALL` moving from tactical `APRSCG`/`APZACG` to `<licensedCall>-<SERVICE_SSID>`, and add
  `aprsCall` to `/.well-known` + an optional `fed_peers` column (pairs T4.2). Small, config-shaped.

**7d. Three genuinely UNRESOLVED open decisions (need OE8APR input, not code):**
1. **Legacy APRScaching data (`docs/04` M3).** "Do you have the original APRScaching DB?" — its
   import/preservation is still unresolved. APRScaching is the owner's own work (per `CLAUDE.md` IP),
   so this is a *data-preservation* decision, not a blocker. **Decide before/at Stage 4** whether v1
   imports legacy data or starts clean.
2. **TOCALL permanent registration (`docs/19`).** Today's `APZACG` is self-assigned/experimental;
   registering a permanent `APAC…` via `aprsorg/aprs-deviceid` is an **external admin** action — start
   it early (lead time), ship v1 on `APZACG` if it isn't granted yet.
3. **ARDC fiscal sponsor (`docs/12`).** The ARDC grant needs an Austrian ham-club fiscal sponsor
   (e.g. ÖVSV) — **admin/non-code**, parallel to the build, not on the critical path.

**7e. Additional explicitly-deferred (post-1.0) items the sweep surfaced** — append to §2's reserved
list: `docs/19` **LoTW-TLS** APRS-IS auth (P4); `docs/08` RF-modes **eval backlog** (Reticulum read,
APRS12c spec swap, LoRa-APRS, **Dire Wolf independent IGate** — a Tier-A corroboration enabler worth
a post-1.0 look, WSPR, APRStt); `docs/24` **T3** HUD flourishes (TUI monitor / CP437 variants) can
trail the T1/T2 theme core if time-boxed; `docs/04` **M6** headroom (HF ports, Neon/PostGIS, multi-
region). None gate v1.
- **Config override layer (`instance_config`)** — a thin DB KV table as a runtime override tier for
  **non-secret** operator knobs that are env-only today (e.g. `min_trust` policy, corroboration quorum,
  NODES/broadcast intervals, feature toggles), resolving **`instance_config` → env default → hard-coded
  default** via a small `cfg(env, key)` helper. Sysop-gated writes surfaced in Instance Admin with the
  effective source ("from env" vs "overridden"). MUST NOT shadow a secret or identity key
  (`INGEST_SECRET`/`FED_PRIVATE_KEY`/`ADMIN_CALLSIGNS` stay strictly env — a security property, not a
  gap). Generalises the pattern already used for partners/peers/rules. Full rationale in `docs/23`
  "Config model". **Not a v1.0 gate.**

**7f. Launch-gating recheck (for the public-domain go-live, not the build):** ADR-3 source link ✅ and
F4 federation trust ✅ are **done**; the only outstanding launch-gate is **`docs/18` L1** (`.com`→`.net`
301 + rpId binding), handled as the Stage 4b deployment task. There is **no code blocker** to tagging
v1.0.0 — only the chosen feature scope (§3) and that deployment redirect.

**7g. Shipped SINCE this roadmap was consolidated (reconcile in Stage 4a; none add a v1.0 blocker).**
The tree has moved past §1's baseline with several coherent tracks — all built + tested, all within the
existing scope, none changing the critical path (Stage 3 → 4):
- **Workbench-is-an-app-launcher IA reorg** — the Workbench is now a pure launcher; APRS/APRScaching
  *functionality* moved OUT of it: platform config → **Settings** (Connections & sources / Network /
  Notifications), live stations → a map layer + station sheet, and **APRS Messages** became a first-class
  platform surface (its own rail surface, never folded into BBS). Every launcher app opens its own surface.
  Governed by `.claude/rules/ui-ux.md`; matches `docs/06` §15's "borrow patterns, not the skin."
- **Account UI-preferences sync** — `account_prefs` (migration `0039`) + `GET/PUT /api/prefs`: pinned
  apps, theme, units, basemap, saved views follow the account across devices (ADR-1/2 scoped by
  `account_id`; inside the GDPR export/erase). *This is the substrate Stage 3's theme selection persists
  through* — the theme toggle writes here.
- **BPQ-parity deep RF wiring (`docs/29`)** — went well beyond Stage 1's P4: the connected-mode session
  server (AX.25 **and** NET/ROM L4 inbound), CachedBbsStore for inbound BBS-over-RF, FBB forwarding
  scheduler + gateway pool, NET/ROM circuit / node / L3 switch / connect-through + multi-hop sequencer,
  connected-mode + viscous digi, AXUDP port. All pure cores loopback-tested; the actual radio legs are
  **validate-at-deploy** (consistent with §1's "RF link behaviour validate-at-deploy"). Not a v1.0 gate.
- **Demo mode + exhaustive teaser** — a `?demo=` harness (real components + in-process sims) and a
  per-viewport Playwright tour → captioned teaser. This is **Stage 4b infrastructure, pre-built** — but
  the teaser MUST be regenerated against the **final, themed** app, so it stays a Stage 4b task (after
  Stage 3), not a thing to finalize now.

- **Instance-operator (sysop) admin surface** — instance-wide config (federation peers + trust, FBB
  forwarding partners + routing rules, server ingest transports + TAK/CoT feed) was being shown in
  per-user Settings **and its write endpoints were ungated** (any user, even unauthenticated, could add
  forwarding partners / node routes / set peer trust). Fixed: an `ADMIN_CALLSIGNS`-based operator identity
  (`requireSysop` server-side gate on every config write; `GET /api/admin/whoami`), a dedicated sysop-only
  **Instance Admin** panel (top-bar 🛡, revealed only to operators), and per-user Settings trimmed to the
  user's own account/radio/notifications. `.claude/rules/ui-ux.md` §5 updated to codify the split. This was
  a real authorization hole, now closed — worth a security note at release.

**7h. Second docs re-sweep (post-Stage-1/2/3) — items the roadmap didn't explicitly carry.** A fresh read
of `docs/11–29` against this plan surfaced a handful of in-scope-adjacent tasks not clearly captured above.
None are hard v1 blockers; classified for an owner call:
- **Off-grid field-station mode (`docs/16` seams A–D) — ✅ BUILT.** Pure `field.ts` core + a client
  `fieldStation` sink (A local map+inbox), H5-gated message TX + one-tap ACK (B), `listCaches` offline
  write-through/read-fallback + "Download this area" (C), and "Sync N heard" replay (D). Radio TX + tile
  precache are validate-at-deploy.
- **Gated user-TX path A (`docs/19` P3) — ✅ BUILT.** `POST /api/tx/aprs` (control-verification-gated)
  enqueues a beacon/message under the user's own call; the box injects it third-party (`thirdPartyEncap`).
  `aprsPasscode()` added. Tested; the APRS-IS write is validate-at-deploy.
- **Web Audio DSP front-ends for the CW/PSK31 decoders (`docs/28` §6)** — makes F-5 usable on live signal,
  not just as a pure codec. → **post-1.0** (audio pipeline; validate-at-deploy today).
- **Federation loose ends (`docs/15`)** — automatic `rep_failed` **contradiction signal** (closes the peer
  auto-promotion loop, T1.1/T4.3); **live re-homing of mirrored find attribution + a "homed at `<instance>`" UI**
  (T3.2); **DNS-`TXT` registry anchor** (T4.2). → **post-1.0** (small, F-tier is already launch-complete).
- **Tools `map` host surface (`docs/28`)** — capability declared, no renderer. → **post-1.0.**
- **PC/FlexNet native driver transport (`docs/27` O7)** — listed in O7 but absent from the built P1.h driver
  set (the FlexNet-*derived* autorouting did land in P4). → **post-1.0 / optional.**

**Net effect on the plan.** With **Stages 0, 1, 2, and 3 all DONE**, there is **no remaining feature stage**
before the tag — only **Stage 4 (release prep)**: 4a cleanup/SPDX/doc-archive, 4b landing content +
marketing splash + the L1 `.com`→`.net` redirect, 4c the ham-facing README, 4d code-quality + CHANGELOG +
**tag `v1.0.0`**. The §7h items above are owner-scope calls, not blockers. **Recommended next build:
Stage 4** (start with 4a cleanup + 4c README, which don't depend on final captures; 4b's teaser/screenshots
regenerate against the now-final themed app).
