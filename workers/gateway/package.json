{
  "name": "@aprsweb/gateway",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc -p tsconfig.json --noEmit",
    "migrate": "wrangler d1 migrations apply aprscaching --local",
    "test": "vitest run"
  },
  "dependencies": { "@aprsweb/aprs": "workspace:*", "@aprsweb/shared": "workspace:*" },
  "devDependencies": { "@cloudflare/workers-types": "^4.20241127.0", "wrangler": "^3.80.0", "typescript": "^5.5.0", "vitest": "^1.6.0" }
}
