{
  "name": "@aprscaching/node-gateway",
  "version": "0.0.0",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "description": "Portable self-host runtime: the aprscaching gateway on Node + SQLite (no Cloudflare).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aprscaching/gateway": "workspace:*",
    "@aprscaching/shared": "workspace:*",
    "better-sqlite3": "^13.0.3",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.2.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.23.12",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  },
  "private": true
}
